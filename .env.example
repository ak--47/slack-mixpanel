# Mixpanel Configuration (REQUIRED)
mixpanel_token=your-mixpanel-token-here
mixpanel_secret=your-mixpanel-secret-here

# Slack API Tokens (REQUIRED)
slack_bot_token=xoxb-your-bot-token-here
slack_user_token=xoxp-your-user-token-here

# Service Configuration (REQUIRED)
service_name=slack-mixpanel-analytics

# Slack Configuration
slack_prefix=https://your-workspace.slack.com/archives
company_domain=your-company.com

# Mixpanel Group Configuration
channel_group_key=channel_id
channel_datagroup_id=your-datagroup-id

# Environment Configuration
NODE_ENV=dev
PORT=8080
CONCURRENCY=2

# Google Cloud Configuration (for cloud storage)
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json