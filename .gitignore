# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
CLAUDE.md
# Environment variables and credentials
.env
.env.*
!.env.example
.env.yaml
.env.yml

# Temporary files
tmp/
*.tmp
*.temp

# Logs
logs
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files (except launch.json which we want to keep)
.vscode/settings.json
.vscode/tasks.json
.vscode/extensions.json
.idea/

# Google Cloud credentials and service accounts
*.json
!package*.json
!cloudbuild.yaml
!service.yaml
service-account*.json
*-credentials.json
gcloud-credentials.json

# Old code (ignore for now)
old/

# Build artifacts
dist/
build/

# Docker files (not needed with Cloud Build)
Dockerfile*
.dockerignore